import React from 'react';
import '../assets/css/Total.css';

const Total = (props) => {
    return (
        <div id="total">
            Total : {props.total}
        </div>
    )
}

export default Total
